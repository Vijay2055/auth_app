import 'package:flutter/material.dart';

class HoldingDetailScreen extends StatelessWidget {
  const HoldingDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}